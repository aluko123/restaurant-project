import { useEffect, useState } from "react";
import type { ApiRequest } from "./SalesWorkspace";

export type OrderGuideLine={id:string;inventoryItemId:string;inventoryItemName:string;countUnit:string;countedQuantity:string;parLevel:string;shortage:string;supplierName:string|null;orderUnit:string;conversion:string;suggestedOrderQuantity:string;orderQuantity:string;receivedQuantity:string|null;receiptStatus:string|null};
export type OrderGuide={id:string;sourceCountId:string;status:"draft"|"ordered"|"received"|"cancelled";revision:number;createdAt:string;updatedAt:string;orderedAt:string|null;receivedAt:string|null;cancelledAt:string|null;lines:OrderGuideLine[]};

function formatQuantity(value:string){const [whole,fraction]=value.split(".");if(fraction===undefined)return value;const trimmed=fraction.replace(/0+$/,"");return trimmed?`${whole}.${trimmed}`:whole}
function displayGuide(guide:OrderGuide):OrderGuide{return {...guide,lines:guide.lines.map(line=>({...line,countedQuantity:formatQuantity(line.countedQuantity),parLevel:formatQuantity(line.parLevel),shortage:formatQuantity(line.shortage),conversion:formatQuantity(line.conversion),suggestedOrderQuantity:formatQuantity(line.suggestedOrderQuantity),orderQuantity:formatQuantity(line.orderQuantity),receivedQuantity:line.receivedQuantity===null?null:formatQuantity(line.receivedQuantity)}))}}

export function OrderGuidePanel({guide,manager,request,onChange}:{guide:OrderGuide;manager:boolean;request:ApiRequest;onChange:(guide:OrderGuide|null,notice?:string)=>void}){
 const [draft,setDraft]=useState(()=>displayGuide(guide)),[busy,setBusy]=useState(false),[error,setError]=useState(""),[notice,setNotice]=useState(""),[dirty,setDirty]=useState(false),[receive,setReceive]=useState<Record<string,{selected:boolean;quantity:string}>>({});
 useEffect(()=>{const displayed=displayGuide(guide);setDraft(displayed);setDirty(false);setReceive(Object.fromEntries(displayed.lines.filter(x=>x.receivedQuantity===null).map(x=>[x.id,{selected:false,quantity:x.orderQuantity}])));},[guide]);
 const edit=(id:string,patch:Partial<OrderGuideLine>)=>{setDraft(x=>({...x,lines:x.lines.map(line=>line.id===id?{...line,...patch}:line)}));setDirty(true)};
 async function save(){const next=await request<OrderGuide>(`/v1/order-guides/${draft.id}`,{method:"PUT",body:JSON.stringify({revision:draft.revision,lines:draft.lines.map(({id,supplierName,orderUnit,conversion,orderQuantity})=>({id,supplierName:supplierName?.trim()||null,orderUnit,conversion,orderQuantity}))})});setDraft(next);setDirty(false);onChange(next);return next}
 async function act(action:"save"|"ordered"|"cancel"){setBusy(true);setError("");setNotice("");try{if(action==="save"){await save();setNotice("Order guide saved.");return}if(action==="cancel"){if(!window.confirm("Cancel this order guide? This cannot be undone."))return;await request(`/v1/order-guides/${draft.id}/cancel`,{method:"POST",body:JSON.stringify({revision:draft.revision})});onChange(null,"Order guide cancelled.");return}const current=dirty?await save():draft;const next=await request<OrderGuide>(`/v1/order-guides/${draft.id}/ordered`,{method:"POST",body:JSON.stringify({revision:current.revision})});onChange(next,"Order recorded. Inventory quantities remain based on the last physical count.")}catch(reason){setError(reason instanceof Error?reason.message:"The order guide couldn't be updated. Try again.")}finally{setBusy(false)}}
 async function submitReceipt(){const lines=Object.entries(receive).filter(([,x])=>x.selected).map(([id,x])=>({id,receivedQuantity:x.quantity.trim()}));if(!lines.length){setError("Select at least one line to receive.");return}setBusy(true);setError("");try{const next=await request<OrderGuide>(`/v1/order-guides/${draft.id}/receive`,{method:"POST",body:JSON.stringify({lines})});if(next.status==="received")onChange(null,"Receipt recorded. Complete another physical count before creating a new guide.");else onChange(next,"Selected receipt lines recorded.")}catch(reason){setError(reason instanceof Error?reason.message:"The receipt couldn't be recorded. Check quantities and try again.")}finally{setBusy(false)}}
 const ordered=draft.status==="ordered", unresolved=draft.lines.filter(x=>x.receivedQuantity===null);
 return <section className="order-guide-panel" aria-labelledby="order-guide-heading"><p className="section-code">Next purchasing task</p><h2 id="order-guide-heading">{ordered?"Receive order":"Review order guide"}</h2><p>{ordered?"This records an order placed outside Parline and does not change inventory. Select only the lines received now; enter 0 when an item is missing.":"Suggestions compare your latest completed physical count with each item's par level. Only items counted below par appear here."}</p>{!manager&&!ordered&&<p className="review-warning">A manager must review, edit, order, or cancel this draft.</p>}{draft.lines.map(line=><article className="guide-line" key={line.id}><div><h3>{line.inventoryItemName}</h3><dl className="guide-evidence"><div><dt>Last counted</dt><dd>{line.countedQuantity} {line.countUnit}</dd></div><div><dt>Par level</dt><dd>{line.parLevel} {line.countUnit}</dd></div><div><dt>Below par by</dt><dd>{line.shortage} {line.countUnit}</dd></div><div><dt>Suggested order</dt><dd>{line.suggestedOrderQuantity} {line.orderUnit}</dd></div></dl></div>{!ordered&&manager?<div className="inventory-form-fields"><label>Supplier<input value={line.supplierName??""} placeholder="Supplier not set" maxLength={120} onChange={e=>edit(line.id,{supplierName:e.target.value})}/></label><label>Order in<input value={line.orderUnit} maxLength={40} onChange={e=>edit(line.id,{orderUnit:e.target.value})}/></label><label>Count units in one order unit <span>Measured in {line.countUnit}</span><input inputMode="decimal" value={line.conversion} onChange={e=>edit(line.id,{conversion:e.target.value})}/></label><label>Quantity to order<input inputMode="decimal" value={line.orderQuantity} onChange={e=>edit(line.id,{orderQuantity:e.target.value})}/></label></div>:ordered&&line.receivedQuantity===null?<div className="receive-fields"><label className="active-toggle"><input type="checkbox" checked={receive[line.id]?.selected??false} onChange={e=>setReceive(x=>({...x,[line.id]:{...(x[line.id]??{quantity:line.orderQuantity}),selected:e.target.checked}}))}/> Receive this line</label><label>Received quantity<input inputMode="decimal" disabled={!receive[line.id]?.selected} value={receive[line.id]?.quantity??line.orderQuantity} onChange={e=>setReceive(x=>({...x,[line.id]:{selected:x[line.id]?.selected??false,quantity:e.target.value}}))}/></label></div>:<p><strong>{line.supplierName||"Supplier not set"}</strong> · Order {line.orderQuantity} {line.orderUnit}{line.receivedQuantity!==null?` · Received ${line.receivedQuantity}`:""}</p>}</article>)}{error&&<p className="form-error" role="alert">{error}</p>}{notice&&<p className="success-notice" role="status">{notice}</p>}{ordered?<button className="ledger-button" type="button" disabled={busy||unresolved.length===0} onClick={()=>void submitReceipt()}>{busy?"Recording…":"Record selected receipt"}</button>:manager&&<div className="guide-actions"><button className="file-button" disabled={busy||!dirty} type="button" onClick={()=>void act("save")}>{dirty?"Save changes":"Changes saved"}</button><button className="ledger-button" disabled={busy} type="button" onClick={()=>void act("ordered")}>{busy?"Working…":"Mark as ordered"}</button><button className="text-button" disabled={busy} type="button" onClick={()=>void act("cancel")}>Cancel guide</button></div>}</section>
}
